// Placeholder — replaced by the feature task that owns this route.
import { EmptyState } from '@/components/EmptyState.tsx';

export function CartPage() {
  return <EmptyState title="Your cart" />;
}
